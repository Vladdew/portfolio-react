import { useTranslation } from "react-i18next";
import Technologien from "../technologienCarousel";
import "./index.scss";

const Skillset = () => {
  const { t } = useTranslation();
  return <div className="skillset"></div>;
};

export default Skillset;
